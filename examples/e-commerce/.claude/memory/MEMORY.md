# Memory

> Read at the start of every session. Rules, pipeline and style live in CLAUDE.md — do not repeat
> them here.
> **Single file. Do not shard into separate notes** (`.claude/hooks/memory-location.sh` pins the
> location; this header pins the shape).
> Write only durable, non-obvious facts. No play-by-play, no commit hashes, no "the user said X" —
> those live in git history.

---

## Project

<!-- What this codebase is, and the one or two things a newcomer gets wrong about it. -->

---

## Toolchain / environment facts

<!-- Package manager, container gates, commands that look right but are not. -->

---

## Traps discovered the hard way

<!-- Each entry: the symptom, the real cause, and the rule that prevents a repeat. -->

---

## Open work / known debt

<!-- Deferred decisions with enough context to resume them cold. -->
