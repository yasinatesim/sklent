// The IDLE branch of a STATE_VIEWS map: a real component reference that renders nothing,
// so the map stays uniform instead of sprouting a null special case.
const NullView = () => null;

export default NullView;
